version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.17
  build:
    commands:
      - echo Building the Go code...
      - cd $CODEBUILD_SRC_DIR/kickshaw-coin
      - go build
artifacts:
  base-directory: kickshaw-coin
  files:
    - kickshaw-coin
